<div>
    <!-- cart btn -->
    <div class="cart-btn">
        <a href="{{{route('cart')}}}" class="theme-btn cart-toggler"><span
                class="flaticon-shopping-cart"></span></a>
        <span>{{$count}}</span>
    </div>
</div>
